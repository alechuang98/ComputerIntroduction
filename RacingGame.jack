class RacingGame{
	field Scene scene;
	field int T;
	constructor RacingGame new(){
		let scene = Scene.new(0 ,0, 0, 0, 0);
		let T=1000;
		while(T>0){
			let T=(T-1);
			do scene.update();
			do scene.draw();
			do Sys.wait(150);
		}
		return this;
	}
}